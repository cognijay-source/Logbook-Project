Check the current git status and create a new feature branch.

Steps:

1. Run `git status` to check for uncommitted changes
2. If there are uncommitted changes, warn me before proceeding
3. Ask me for a short description of the feature/fix
4. Create and checkout a new branch following the project naming convention

<!-- TODO: Update with preferred branch naming convention once confirmed -->
